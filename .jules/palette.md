## 2024-05-18 - ARIA labels for icon-only buttons
**Learning:** Found multiple instances of icon-only buttons (like settings close, prompt options) missing `aria-label` attributes.
**Action:** Always ensure `aria-label` is present on any button that only contains an icon.
